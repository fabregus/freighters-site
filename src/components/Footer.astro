---
// src/components/Footer.astro

const phone = "+30 210 4444856";
const email = "ops@freighters.gr";
const addressLines = [
  "El. Venizelos Airport",
  "Spata, Bldg. 27, 2nd Floor - Off. 420",
  "Athens, Greece",
];
---

<footer id="contact" class="relative overflow-hidden bg-[#061A2A] text-white">
  <div class="container-site relative pt-20 pb-28">
    <div class="grid gap-12 md:grid-cols-2">
      <!-- Left column -->
      <div class="space-y-10">
        <div>
          <p class="text-[34px] font-light tracking-[0.06em] text-white">
            GET IN TOUCH
          </p>
        </div>

        <div>
          <p class="text-sm text-white/70">Call us</p>
          <a
            href={`tel:${phone.replace(/\s+/g, "")}`}
            class="mt-1 inline-block text-lg text-white/95 hover:text-white transition"
          >
            {phone}
          </a>
        </div>

        <div>
          <p class="text-sm text-white/70">Email us</p>
          <a
            href={`mailto:${email}`}
            class="mt-1 inline-block text-lg text-white/95 hover:text-white transition"
          >
            {email}
          </a>
        </div>

        <div>
          <p class="text-sm text-white/70">Address</p>
          <div class="mt-1 space-y-1 text-white/95">
            {addressLines.map((line) => <p class="text-base">{line}</p>)}
          </div>
        </div>
      </div>

      <!-- Right column -->
      <div class="flex items-center justify-end">
        <div
          class="text-[#F5B400] text-[72px] leading-none font-semibold tracking-tight"
        >
          freighters
        </div>
      </div>
    </div>
  </div>

  <!-- Background watermark -->
  <div
    class="pointer-events-none absolute -bottom-24 left-1/2 -translate-x-1/2 select-none"
  >
    <span
      class="text-[#F5B400] opacity-[0.1] font-bold tracking-tight text-[220px] leading-none"
    >
      freighters
    </span>
  </div>
</footer>
